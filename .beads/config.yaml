# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Issue prefix for this repository (used by bd init)
# If not set, bd init will auto-detect from directory name
# Example: issue-prefix: "myproject" creates issues like "myproject-1", "myproject-2", etc.
# issue-prefix: ""

# Use no-db mode: JSONL-only, no Dolt database
# When true, bd will use .beads/issues.jsonl as the source of truth
# no-db: false

# Enable JSON output by default
# json: false

# Feedback title formatting for mutating commands (create/update/close/dep/edit)
# 0 = hide titles, N > 0 = truncate to N characters
# output:
#   title-length: 255

# Default actor for audit trails (overridden by BEADS_ACTOR or --actor)
# actor: ""

# Export events (audit trail) to .beads/events.jsonl on each flush/sync
# When enabled, new events are appended incrementally using a high-water mark.
# Use 'bd export --events' to trigger manually regardless of this setting.
# events-export: false

# Multi-repo configuration (experimental - bd-307)
# Allows hydrating from multiple repositories and routing writes to the correct database
# repos:
#   primary: "."  # Primary repo (where this database lives)
#   additional:   # Additional repos to hydrate from (read-only)
#     - ~/beads-planning  # Personal planning repo
#     - ~/work-planning   # Work planning repo

# SHARED SERVER, SET 2026-09-03 WHEN THIS PROJECT WAS CONSOLIDATED. Its `oas` database now lives
# in `~/.beads/shared-server/dolt/` alongside every other project's, served by one `dolt sql-server`
# on port 3308 rather than by a server of its own.
#
# THIS FLAG IS THE WHOLE MECHANISM, AND WITHOUT IT THE CONSOLIDATION SILENTLY UNDOES ITSELF. With
# it, bd resolves the port from `~/.beads/shared-server/dolt-server.port` and this project's own
# `.beads/dolt-server.port` becomes inert. Without it, bd defaults to per-project, AUTO-STARTS AN
# EPHEMERAL SERVER ON A RANDOM PORT, and writes that port into the project's port file — after
# which bd connects to a server holding no databases and reports `database "oas" not found`.
#
# THAT IS NOT HYPOTHETICAL. The `Obsidian` project was found in exactly that state on 2026-09-03:
# its data had been moved into the shared server in March, its config was never told, and its port
# file pointed at an auto-started empty server. Its tracker was unusable and nothing announced it.
# `atomyst` was found one auto-start away from the same fate. Diagnosed with the web-app session,
# which has carried this flag since March and whose port file has survived six months untouched.
dolt.shared-server: true

# JSONL backup (periodic export for off-machine recovery)
#
# SET EXPLICITLY ON 2026-08-30, NOT LEFT TO AUTO-DETECTION, because auto-detection had it
# wrong. `bd backup status` reported `enabled=false (auto: no git remote)` while `origin`
# plainly existed and had been pushed to all day. Whatever it inferred, it inferred once and
# never revisited — so the snapshot sat ten hours stale at 32 issues while the tracker held 53,
# and the twenty-one missing ones were the arguments cut out of seven design documents that same
# day. They existed only in `.beads/dolt/`, which `.beads/.gitignore` excludes.
#
# The failure was silent in the way this project cares about: nothing was broken, a command
# simply was not running, and the only symptom was a number in a status line nobody had read.
backup:
  enabled: true
  interval: 15m
  git-push: true     # publishes to the `beads-backup` branch; see `bd backup export-git`

# Integration settings (access with 'bd config get/set')
# These are stored in the database, not in this file:
# - jira.url
# - jira.project
# - linear.url
# - linear.api-key
# - github.org
# - github.repo
